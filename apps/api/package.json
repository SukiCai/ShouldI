{
  "name": "@shouldi/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "ShouldI HTTP API — Explore feed, Hermes discovery, briefing endpoint.",
  "scripts": {
    "predev": "npm run build -w @shouldi/contracts",
    "dev": "tsx watch src/index.ts",
    "prestart": "npm run build -w @shouldi/contracts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.9",
    "@shouldi/contracts": "*",
    "hono": "^4.11.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "tsx": "^4.21.0",
    "typescript": "^5.9.2"
  }
}
